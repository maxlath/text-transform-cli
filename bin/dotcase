#!/usr/bin/env node
const transformText = require('../lib/transform_text')
transformText({
  transform: text => {
    return text
    .replace(/[^\p{Letter}\p{Number}]+/ug, '.')
    .replace(/\.+/g, '.')
    .replace(/^\./, '')
    .replace(/\.$/, '')
  }
})
