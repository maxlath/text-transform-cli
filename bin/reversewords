#!/usr/bin/env node
const transformText = require('../lib/transform_text')
const reverseWords = text => text.split(' ').reverse().join(' ')
transformText({ transform: reverseWords, canHandleChunks: false })
